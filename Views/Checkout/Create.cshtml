<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Move Out | Cosy Commune</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-slate-50 text-slate-900 min-h-screen">

    <div id="mainContainer" class="max-w-md mx-auto px-6 py-12">
        <!-- View 1: Notice Form -->
        <div id="noticeSection">
            <div class="mb-10">
                <a href="dashboard.html" class="text-slate-400 font-bold text-sm hover:text-rose-600">
                    <i class="fa-solid fa-arrow-left mr-2"></i> Cancel
                </a>
                <h1 class="text-3xl font-black mt-4">Move Out Notice</h1>
                <p class="text-slate-500">Initiate your checkout process.</p>
            </div>

            <form id="checkoutForm" action="dashboard.html" method="GET" class="bg-white p-8 rounded-[2.5rem] border border-slate-200 shadow-sm space-y-8">
                <div class="space-y-4">
                    <label class="block">
                        <span class="text-xs font-black uppercase text-slate-400 tracking-widest">Proposed Vacate Date</span>
                        <input type="date" id="vacateDate" name="vacateDate" required class="mt-2 w-full p-4 bg-slate-50 rounded-2xl font-bold focus:ring-2 focus:ring-rose-500 outline-none">
                    </label>

                    <label class="block">
                        <span class="text-xs font-black uppercase text-slate-400 tracking-widest">Reason for Leaving</span>
                        <select id="reason" name="reason" required class="mt-2 w-full p-4 bg-slate-50 rounded-2xl font-bold focus:ring-2 focus:ring-rose-500 outline-none">
                            <option value="">Select reason...</option>
                            <option value="relocation">Relocating</option>
                            <option value="other">Other</option>
                        </select>
                    </label>
                </div>

                <button type="submit" class="w-full py-5 bg-rose-600 text-white rounded-2xl font-black shadow-lg shadow-rose-100 hover:bg-rose-700 transition-colors">
                    Initiate Checkout
                </button>
            </form>
        </div>

        <!-- View 2: Confirmation Section -->
        <div id="confirmationSection" class="hidden text-center py-10 animate-in fade-in duration-500">
            <div class="w-20 h-20 bg-rose-100 text-rose-600 rounded-full flex items-center justify-center mx-auto mb-6">
                <i class="fa-solid fa-paper-plane text-3xl"></i>
            </div>
            
            <h2 class="text-3xl font-black mb-2">Notice Submitted</h2>
            <p class="text-slate-500 mb-8 px-4 leading-relaxed">
                Your intent to vacate on <span id="confDate" class="text-slate-900 font-bold"></span> has been recorded.
            </p>

            <div class="bg-rose-50 border border-rose-100 rounded-2xl p-6 mb-8 text-left">
                <div class="flex items-start gap-4">
                    <div class="mt-1 text-rose-600">
                        <i class="fa-solid fa-circle-info"></i>
                    </div>
                    <div>
                        <h4 class="font-bold text-rose-900 text-sm mb-1">What happens next?</h4>
                        <p class="text-xs text-rose-700 leading-relaxed">
                            A manager will reach out within 48 hours to schedule your final inspection. You will be notified of the progress via your building inbox.
                        </p>
                    </div>
                </div>
            </div>

            <a href="dashboard.html" class="inline-block w-full py-5 bg-slate-900 text-white rounded-2xl font-black hover:bg-slate-800 transition-colors">
                Return to Dashboard
            </a>
        </div>
    </div>

    <script>
        document.getElementById('checkoutForm').addEventListener('submit', function(e) {
            // We prevent the actual redirect to show the confirmation state first
            e.preventDefault();
            
            const date = document.getElementById('vacateDate').value;
            const reason = document.getElementById('reason').value;

            if (!date || !reason) return;

            // Log for developer context
            console.log("Initiating Checkout Notice:", { date, reason });

            // Update confirmation text
            document.getElementById('confDate').innerText = new Date(date).toLocaleDateString('en-GB', { 
                day: 'numeric', 
                month: 'long', 
                year: 'numeric' 
            });

            // Switch views
            document.getElementById('noticeSection').classList.add('hidden');
            document.getElementById('confirmationSection').classList.remove('hidden');
            
            window.scrollTo({ top: 0, behavior: 'smooth' });
        });
    </script>
</body>
</html>